import SwiftUI
import shared

struct FeaturedScreen: View {
    let accountType: AccountType
    
    var body: some View {
        Text("精选页面")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
