import shared
import SwiftUI

struct FeaturedScreen: View {
    let accountType: AccountType

    var body: some View {
        Text("精选页面")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
