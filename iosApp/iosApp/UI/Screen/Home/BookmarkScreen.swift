import SwiftUI
import shared

struct BookmarkScreen: View {
    let accountType: AccountType
    
    var body: some View {
        Text("书签页面")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
