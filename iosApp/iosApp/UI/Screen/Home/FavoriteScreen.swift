import SwiftUI
import shared

struct FavoriteScreen: View {
    let accountType: AccountType
    
    var body: some View {
        Text("收藏页面")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
